[tool.poetry]
name = "fernet-dbfp"
version = "1.2.0"
description = "基于Fernet算法的pem证书私钥数据库"
authors = ["Nya-WSL"]
license = "MIT"
readme = "README.md"

[tool.poetry.dependencies]
python = "3.11.5"
cryptography = "^42.0.2"
pyinstaller = "^6.11.1"
pyyaml = "^6.0.2"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
